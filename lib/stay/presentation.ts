export const STAY_STATUSES: Record<string,string> = { pending_admin_review:"予約申請",admin_reviewing:"管理者確認中",awaiting_guest_confirmation:"顧客確認待ち",confirmed:"予約確定",payment_pending:"支払い待ち",paid:"支払い済み",check_in_scheduled:"チェックイン予定",checked_in:"宿泊中",checked_out:"チェックアウト済み",completed:"完了",guest_cancelled:"顧客キャンセル",admin_cancelled:"管理者キャンセル",expired:"期限切れ",no_show:"無断不泊" };
export const PAYMENT_STATUSES: Record<string,string> = { unpaid:"未払い",payment_pending:"確認中",paid:"支払い済み",refunded:"返金済み",partially_refunded:"一部返金" };
export function yen(value:number|null|undefined){return new Intl.NumberFormat("ja-JP",{style:"currency",currency:"JPY",maximumFractionDigits:0}).format(value??0)}
export function stayDate(value:string){const [year,month,day]=value.split("-").map(Number);return new Intl.DateTimeFormat("ja-JP",{year:"numeric",month:"short",day:"numeric",timeZone:"UTC"}).format(new Date(Date.UTC(year,month-1,day)))}
export function stayPreviousDate(value:string){const [year,month,day]=value.split("-").map(Number);const date=new Date(Date.UTC(year,month-1,day));date.setUTCDate(date.getUTCDate()-1);return new Intl.DateTimeFormat("ja-JP",{year:"numeric",month:"short",day:"numeric",timeZone:"UTC"}).format(date)}
