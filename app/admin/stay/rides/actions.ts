"use server";
import { revalidatePath } from "next/cache";
import { redirect } from "next/navigation";
import { requireAdminUser } from "@/lib/auth/require-admin";
import { createSupabaseAdminClient } from "@/lib/supabase/admin";

export async function updateRideDiscount(formData:FormData){const user=await requireAdminUser();const discount=Number(formData.get("discountPercent"));if(!Number.isFinite(discount)||discount<0||discount>100)redirect("/admin/stay/rides?saved=invalid");const {error}=await createSupabaseAdminClient().from("stay_ride_settings").update({discount_percent:discount,updated_at:new Date().toISOString(),updated_by:user.id}).eq("id",true);if(error){console.error("配車割引率を保存できませんでした。",error);redirect("/admin/stay/rides?saved=failed")}revalidatePath("/admin/stay/rides");revalidatePath("/stay/mypage/rides/new");redirect("/admin/stay/rides?saved=success")}
export async function updateRideBooking(formData:FormData){await requireAdminUser();const id=String(formData.get("id")??""),rideDate=String(formData.get("rideDate")??""),departureTime=String(formData.get("departureTime")??""),pickup=String(formData.get("pickupAddress")??"").trim().slice(0,500),destination=String(formData.get("destinationAddress")??"").trim().slice(0,500),total=Number(formData.get("totalAmount")),memo=String(formData.get("adminMemo")??"").trim().slice(0,5000);if(!/^[0-9a-f-]{36}$/i.test(id)||!/^\d{4}-\d{2}-\d{2}$/.test(rideDate)||!/^\d{2}:\d{2}$/.test(departureTime)||pickup.length<2||destination.length<2||!Number.isSafeInteger(total)||total<0)redirect("/admin/stay/rides?saved=invalid");const admin=createSupabaseAdminClient();const {data:current}=await admin.from("stay_ride_bookings").select("payment_status,status").eq("id",id).maybeSingle();if(!current||current.payment_status==="paid"||["completed","customer_cancelled","admin_cancelled"].includes(current.status))redirect("/admin/stay/rides?saved=invalid");const {error}=await admin.from("stay_ride_bookings").update({ride_date:rideDate,departure_time:departureTime,pickup_address:pickup,destination_address:destination,total_amount:total,status:"awaiting_guest_confirmation",admin_memo:memo,updated_at:new Date().toISOString()}).eq("id",id);if(error){console.error("配車予約を更新できませんでした。",error);redirect("/admin/stay/rides?saved=failed")}revalidatePath("/admin/stay/rides");revalidatePath("/stay/mypage/rides");redirect("/admin/stay/rides?saved=success")}
