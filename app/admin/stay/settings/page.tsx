import { createSupabaseAdminClient } from "@/lib/supabase/admin";
export default async function Settings(){const {data:s}=await createSupabaseAdminClient().from('stay_settings').select('*').eq('id',true).single();return <><h1 className="text-3xl font-bold">宿泊設定</h1><div className="mt-6 grid gap-4 rounded-xl bg-white p-6 sm:grid-cols-2"><p>標準チェックイン<br/><b>{s.default_check_in_time}</b></p><p>標準チェックアウト<br/><b>{s.default_check_out_time}</b></p><p>申請有効期限<br/><b>{s.request_expiry_hours}時間</b></p><p>予約受付<br/><b>{s.booking_acceptance_enabled?'有効':'停止中'}</b></p><p className="sm:col-span-2">支払い案内<br/><span className="whitespace-pre-wrap text-sm text-slate-500">{s.payment_instructions||'未設定'}</span></p></div></>}
