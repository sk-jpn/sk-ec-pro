"use client";import { useActionState } from "react";import { Button } from "@/components/ui/button";import { Input } from "@/components/ui/input";import { StayLocalized } from "../../stay-language";import { requestBooking,type BookingState } from "./actions";
const initial:BookingState={success:false,message:""};
export function BookingForm({listingId,checkIn,checkOut,guests,name,email,phone}:{listingId:string;checkIn:string;checkOut:string;guests:number;name:string;email:string;phone:string}){const [state,action,pending]=useActionState(requestBooking,initial);return <StayLocalized><form action={action} className="mt-6 grid gap-4 rounded-2xl bg-white p-6 shadow-sm"><input type="hidden" name="listingId" value={listingId}/><div className="grid gap-4 sm:grid-cols-3"><label className="text-sm font-medium">チェックイン<Input name="checkIn" type="date" defaultValue={checkIn} required/></label><label className="text-sm font-medium">チェックアウト<Input name="checkOut" type="date" defaultValue={checkOut} required/></label><label className="text-sm font-medium">宿泊人数<Input name="guests" type="number" min="1" defaultValue={guests} required/></label></div><div className="grid gap-4 sm:grid-cols-2"><label className="text-sm font-medium">氏名<Input name="name" defaultValue={name} maxLength={100} required/></label><label className="text-sm font-medium">メールアドレス<Input name="email" type="email" defaultValue={email} required/></label><label className="text-sm font-medium">電話番号<Input name="phone" type="tel" defaultValue={phone} required/></label><label className="text-sm font-medium">到着予定時刻<Input name="arrivalTime" type="time" defaultValue="15:00" required/></label></div><label className="text-sm font-medium">宿泊目的（任意）<Input name="purpose" maxLength={500}/></label><label className="text-sm font-medium">備考・追加要望（任意）<textarea name="note" maxLength={2000} className="mt-1 min-h-24 w-full rounded-lg border border-slate-200 p-3"/></label><label className="flex items-center gap-2 text-sm"><input type="checkbox" name="airbnb"/>以前宿泊したことがあります。</label><p className="rounded-lg bg-amber-50 p-4 text-sm text-amber-800">支払い方法は、管理者が予約内容を確認した後にご案内します。</p>{state.message&&<p role="alert" className="text-sm text-red-600">{state.message}</p>}<Button size="lg" disabled={pending}>{pending?"送信中…":"予約をリクエストする"}</Button></form></StayLocalized>}
