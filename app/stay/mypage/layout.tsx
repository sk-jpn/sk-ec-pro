import Link from "next/link";import { requireStayUser } from "@/lib/stay/auth";
export default async function StayMypageLayout({children}:{children:React.ReactNode}){await requireStayUser();return <main className="mx-auto max-w-6xl px-5 py-8 sm:px-8"><nav className="mb-8 flex gap-2 overflow-x-auto rounded-xl bg-white p-2 text-sm font-medium shadow-sm">{[["/stay/mypage","ホーム"],["/stay/mypage/bookings","宿泊予約"],["/stay/mypage/rides","配車予約"],["/stay/messages","メッセージ"],["/stay/profile","プロフィール"]].map(([h,l])=><Link key={h} href={h} className="shrink-0 rounded-lg px-4 py-2 hover:bg-slate-100">{l}</Link>)}</nav>{children}</main>}
