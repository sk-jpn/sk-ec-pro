import Link from "next/link";import { Button } from "@/components/ui/button";import { Card,CardContent } from "@/components/ui/card";import { requireStayUser } from "@/lib/stay/auth";import { STAY_STATUSES,stayDate,yen } from "@/lib/stay/presentation";import { OwnerContact } from "./owner-contact";
export default async function Mypage(){const {customer,supabase}=await requireStayUser();const {data:bookings}=await supabase.from("stay_bookings").select("id,booking_number,check_in_date,check_out_date,total_amount,status,stay_listings(name)").eq("customer_id",customer.id).order("check_in_date",{ascending:true});return <><p className="text-xs font-bold uppercase tracking-[.2em] text-emerald-600">Stay My Page</p><h1 className="mt-2 text-3xl font-bold">{customer.name} 様</h1><OwnerContact/><div className="mt-9 flex items-center justify-between"><h2 className="text-xl font-bold">宿泊予約</h2><Button asChild><Link href="/stay/search">空室を検索</Link></Button></div><div className="mt-4 grid gap-4">{(bookings??[]).map(b=><Card key={b.id}><CardContent className="flex flex-col justify-between gap-4 p-5 sm:flex-row sm:items-center"><div><p className="text-xs font-bold text-emerald-600">{b.booking_number}</p><h3 className="mt-1 font-bold">{(b.stay_listings as unknown as {name:string})?.name}</h3><p className="mt-2 text-sm text-slate-500">{stayDate(b.check_in_date)}〜{stayDate(b.check_out_date)}・{yen(b.total_amount)}</p></div><div><span className="rounded-full bg-slate-100 px-3 py-1 text-xs font-bold">{STAY_STATUSES[b.status]??b.status}</span><Button asChild variant="outline" className="ml-2"><Link href={`/stay/mypage/bookings/${b.id}`}>詳細</Link></Button></div></CardContent></Card>)}{!bookings?.length&&<p className="rounded-xl bg-white p-8 text-center text-slate-500">予約はまだありません。</p>}</div></>}
