import Link from "next/link";
import { notFound } from "next/navigation";
import { Button } from "@/components/ui/button";
import { requireStayUser } from "@/lib/stay/auth";
import { STAY_STATUSES, PAYMENT_STATUSES, stayDate, yen } from "@/lib/stay/presentation";
import { customerBookingAction, selectStayBankTransfer, startStayStripeCheckout } from "./actions";
import { CancelBookingForm } from "./cancel-booking-form";

export default async function BookingDetail({ params, searchParams }: { params: Promise<{ id: string }>; searchParams: Promise<{ created?: string; payment?: string }> }) {
  const { id } = await params; const q = await searchParams; const { customer, supabase } = await requireStayUser();
  const { data: b } = await supabase.from("stay_bookings").select("*,stay_listings(name,code),stay_message_threads(id)").eq("id", id).eq("customer_id", customer.id).maybeSingle();
  if (!b) notFound();
  const listing = b.stay_listings as unknown as { name: string; code: string }; const thread = (b.stay_message_threads as unknown as { id: string }[])?.[0];
  const cancellable = ["pending_admin_review", "admin_reviewing", "awaiting_guest_confirmation"].includes(b.status);
  const payable=["confirmed","payment_pending"].includes(b.status)&&b.payment_status!=="paid";
  return <div className="mx-auto max-w-3xl">{q.created && <p className="mb-5 rounded-xl bg-emerald-50 p-4 text-emerald-800">予約リクエストを受け付けました。内容を確認後、メールでお知らせします。</p>}{q.payment==='stripe_success'&&<p className="mb-5 rounded-xl bg-emerald-50 p-4 text-emerald-800">カード決済を受け付けました。支払い状況は決済確認後に更新されます。</p>}{q.payment==='stripe_cancelled'&&<p className="mb-5 rounded-xl bg-amber-50 p-4 text-amber-800">カード決済をキャンセルしました。再度お支払い方法を選択できます。</p>}{q.payment==='bank'&&<p className="mb-5 rounded-xl bg-blue-50 p-4 text-blue-800">銀行振込を選択しました。下記口座へお振り込みください。</p>}{q.payment==='failed'&&<p className="mb-5 rounded-xl bg-red-50 p-4 text-red-700">支払い処理を開始できませんでした。時間をおいて再度お試しください。</p>}<p className="text-xs font-bold text-emerald-600">{b.booking_number}</p><h1 className="mt-2 text-3xl font-bold">{listing.name}</h1><div className="mt-6 grid gap-4 rounded-2xl bg-white p-6 sm:grid-cols-2"><p>宿泊期間<br /><b>{stayDate(b.check_in_date)}〜{stayDate(b.check_out_date)}</b></p><p>人数<br /><b>{b.guest_count}名・{b.nights}泊</b></p><p>予約状況<br /><b>{STAY_STATUSES[b.status]}</b></p><p>支払い状況<br /><b>{PAYMENT_STATUSES[b.payment_status]}</b></p><p>宿泊料金<br /><b>{yen(b.subtotal)}</b></p><p>追加人数料金<br /><b>{yen(b.additional_guest_fee)}</b></p><p>清掃費<br /><b>{yen(b.cleaning_fee)}</b></p><p className="text-lg">合計<br /><b>{yen(b.total_amount)}</b></p></div>{b.admin_message && <p className="mt-5 whitespace-pre-wrap rounded-xl bg-blue-50 p-5 text-sm">{b.admin_message}</p>}{payable&&<section className="mt-6 rounded-2xl border border-slate-200 bg-white p-5 sm:p-6"><h2 className="text-xl font-bold">お支払い方法</h2><p className="mt-2 text-sm text-slate-500">合計 {yen(b.total_amount)} のお支払い方法を選択してください。</p><div className="mt-5 grid gap-4 sm:grid-cols-2"><div className="rounded-xl border border-slate-200 p-5"><h3 className="font-bold">クレジットカード</h3><p className="mt-2 text-sm leading-6 text-slate-500">Stripeの安全な決済画面へ移動します。カード情報は当サイトに保存されません。</p><form action={startStayStripeCheckout} className="mt-4"><input type="hidden" name="id" value={id}/><Button className="w-full">Stripeで支払う</Button></form></div><div className="rounded-xl border border-slate-200 p-5"><h3 className="font-bold">銀行振込</h3><p className="mt-3 whitespace-pre-line rounded-lg bg-slate-50 p-3 text-sm font-medium leading-7">三菱UFJ銀行<br/>新宿支店<br/>普通 0039565<br/>カミキシンノスケ</p><p className="mt-2 text-xs text-slate-500">振込手数料はお客様負担です。入金確認後に支払い状況を更新します。</p><form action={selectStayBankTransfer} className="mt-4"><input type="hidden" name="id" value={id}/><Button variant="outline" className="w-full">銀行振込を選択</Button></form></div></div></section>}{b.payment_status==='paid'&&<p className="mt-6 rounded-xl bg-emerald-50 p-5 font-bold text-emerald-800">お支払い済みです。ありがとうございました。</p>}<div className="mt-6 flex min-w-0 flex-col gap-3 sm:flex-row sm:flex-wrap">{b.status === "awaiting_guest_confirmation" && <form action={customerBookingAction}><input type="hidden" name="id" value={id} /><input type="hidden" name="action" value="confirm" /><Button>この内容で予約する</Button></form>}{cancellable && <div className="min-w-0 flex-1"><CancelBookingForm id={id} /></div>}{thread && <Button asChild variant="outline"><Link href={`/stay/messages/${thread.id}`}>メッセージ</Link></Button>}</div></div>;
}
