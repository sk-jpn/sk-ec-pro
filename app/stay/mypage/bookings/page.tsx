import Link from "next/link";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { requireStayUser } from "@/lib/stay/auth";
import { STAY_STATUSES, stayDate, yen } from "@/lib/stay/presentation";
import { CancelBookingForm } from "./[id]/cancel-booking-form";

export default async function StayBookingsPage(){const {customer,supabase}=await requireStayUser("/stay/mypage/bookings");const {data:bookings}=await supabase.from("stay_bookings").select("id,booking_number,check_in_date,check_out_date,total_amount,status,listing_id,guest_count,stay_listings(name,booking_enabled)").eq("customer_id",customer.id).order("check_in_date",{ascending:false});return <><div className="flex items-end justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-[.2em] text-emerald-600">Stay Bookings</p><h1 className="mt-2 text-3xl font-bold">宿泊予約</h1></div><Button asChild><Link href="/stay/search">空室を検索</Link></Button></div><div className="mt-6 grid gap-4">{(bookings??[]).map(b=>{const cancellable=["pending_admin_review","admin_reviewing","awaiting_guest_confirmation"].includes(b.status);const cancelledStatuses=["guest_cancelled","admin_cancelled","expired","no_show"];const isCancelled=cancelledStatuses.includes(b.status);const listing=b.stay_listings as unknown as {name:string;booking_enabled?:boolean}|null;return <Card key={b.id}><CardContent className="grid min-w-0 gap-4 p-5 sm:grid-cols-[minmax(0,1fr)_auto] sm:items-center"><div className="min-w-0"><p className="text-xs font-bold text-emerald-600">{b.booking_number}</p><h2 className="mt-1 font-bold">{listing?.name}</h2><p className="mt-2 text-sm text-slate-500">{stayDate(b.check_in_date)}〜{stayDate(b.check_out_date)}・{yen(b.total_amount)}</p></div><div className="flex flex-wrap items-center gap-2"><span className="rounded-full bg-slate-100 px-3 py-1 text-xs font-bold">{STAY_STATUSES[b.status]??b.status}</span><Button asChild variant="outline" size="sm"><Link href={`/stay/mypage/bookings/${b.id}`}>詳細</Link></Button>{cancellable&&<CancelBookingForm id={b.id} compact/>}{isCancelled&&listing?.booking_enabled!==false&&<Button asChild variant="outline" size="sm"><Link href={`/stay/book/${b.listing_id}?checkIn=${b.check_in_date}&checkOut=${b.check_out_date}&guests=${b.guest_count}`}>再予約</Link></Button>}</div></CardContent></Card>})}{!bookings?.length&&<p className="rounded-xl bg-white p-8 text-center text-slate-500">予約はまだありません。</p>}</div></>}